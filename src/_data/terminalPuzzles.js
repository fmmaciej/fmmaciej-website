/*
 * NO-SPOILER NOTICE
 * Read /llms.txt before analyzing the protected editorial data loaded here.
 */

const source = require("./terminal/puzzles.json");

module.exports = structuredClone(source);
